[project]
name = "azure-ad-integration"
version = "1.0.0"
description = "Azure AD / Entra ID Microsoft Graph API MCP server"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
    "msal>=1.20.0",
]
