[project]
name = "netsuite-edi"
version = "1.0.0"
description = "NetSuite EDI analytics MCP server via API Gateway"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]
