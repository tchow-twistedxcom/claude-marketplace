# NetSuite SDF Deployment Environment Variables

# Shared credentials (used across all environments)
TWX_SDF_CERT_ID=your-certificate-id
TWX_SDF_PRIVATE_KEY_PATH=/path/to/your/private-key.pem
TWX_SDF_CI_PASSKEY=your-ci-passkey

# Environment-specific credentials (override shared credentials)
# Sandbox 1
TWX_SDF_SB1_CERT_ID=sb1-cert-id
TWX_SDF_SB1_PRIVATE_KEY_PATH=/path/to/sb1-key.pem

# Sandbox 2
TWX_SDF_SB2_CERT_ID=sb2-cert-id
TWX_SDF_SB2_PRIVATE_KEY_PATH=/path/to/sb2-key.pem

# Production
TWX_SDF_PROD_CERT_ID=prod-cert-id
TWX_SDF_PROD_PRIVATE_KEY_PATH=/path/to/prod-key.pem

# SuiteCloud CLI Configuration (automatically set by tool)
# SUITECLOUD_CI=1

# Optional: Debug mode
# DEBUG=twx-deploy:*
